[package]
name = "benchmarks"
version = "0.1.0"
edition = "2024"

[dependencies]

[build-dependencies]
cmake = "0.1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
rand = "0.8"
interpn = {version = "0.11", features = ["fma"]}

[[bench]]
name = "expfilter"
harness = false
